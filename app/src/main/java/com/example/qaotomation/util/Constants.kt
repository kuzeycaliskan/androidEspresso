package com.example.qaotomation.util

object Constants {
    const val SHARED_PREF_NAME = "QASharedPref"
    const val SHARED_PREF_USERS = "users"

    const val SHARED_PREF_MODE = 0

    const val EMAIL_PATTERN = "[a-zA-Z0-9._-]+@[a-z]+\\.+[a-z]+"

    const val MIN_PASSWORD_LENGTH = 8
}